# PulseDesk UI

React frontend for PulseDesk comment and ticket triage.

## Run

```bash
cp .env.example .env
npm install
npm run dev
```

Open http://localhost:5173

Make sure the Spring Boot API is running on the URL in `VITE_API_URL` (default `http://localhost:8080`).
